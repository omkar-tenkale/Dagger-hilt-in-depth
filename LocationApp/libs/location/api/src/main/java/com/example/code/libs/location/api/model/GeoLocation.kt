package com.example.code.libs.location.api.model

/**
 * Location information does not depend on a particular platform
 */
data class GeoLocation(
    val latitude: Double,
    val longitude: Double
)
